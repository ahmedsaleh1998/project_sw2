@model IEnumerable<library_management.Model.Author>

@{
    ViewBag.Title = "Index_author";
}

<h2>Index_author</h2>

<p>
    @Html.ActionLink("Create New", "Create_author")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Author_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Author_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit_author", new { id = item.Author_Id }) |
            @*@Html.ActionLink("Details", "Details_author", new { id = item.Author_Id }) |*@
            @Html.ActionLink("Delete", "Delete_author", new { id = item.Author_Id })
        </td>
    </tr>
}

</table>
